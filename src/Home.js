import React from "react";
import Visualizer from "./Visualizer";

const Home = () => {
    return (
        <React.Fragment>
            <Visualizer />
        </React.Fragment>
    );
};

export default Home;
