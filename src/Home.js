import React from "react";
import { Heading } from "grommet";

const Home = () => {
    return (
        <Heading level="3" margin="none">
            app body
        </Heading>
    );
};

export default Home;
