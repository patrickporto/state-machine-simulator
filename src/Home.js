import React from "react";
import Renderer from "./CytoscapeRenderer";

const Home = () => {
    return (
        <React.Fragment>
            <Renderer />
        </React.Fragment>
    );
};

export default Home;
