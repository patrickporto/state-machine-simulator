import React from "react";
import { CytoscapeRenderer as Renderer } from "../components";

const Home = () => {
    return (
        <React.Fragment>
            <Renderer />
        </React.Fragment>
    );
};

export default Home;
